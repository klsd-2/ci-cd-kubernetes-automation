apiVersion: v1
kind: Service

metadata:
  name: flask-service

spec:
  selector:
    app: flask-app

  ports:
  - port: {{ .Values.service.port }}
    targetPort: 5000

  type: {{ .Values.service.type }}
